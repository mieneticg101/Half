[workspace]
resolver = "2"
members = [
    "half-core",
    "half-macros",
    "half-cli",
]

[workspace.package]
version = "0.1.0"
edition = "2021"  # Using 2021 as 2024 is not stable yet
authors = ["Half Framework Contributors"]
license = "MIT OR Apache-2.0"
repository = "https://github.com/mieneticg101/Half"
rust-version = "1.75"

[workspace.dependencies]
# Async runtime
tokio = { version = "1.42", features = ["full"] }
hyper = { version = "1.5", features = ["full"] }
hyper-util = { version = "0.1", features = ["full"] }
http-body-util = "0.1"

# Serialization
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

# Security
sha2 = "0.10"
hmac = "0.12"
base64 = "0.22"
htmlescape = "0.3"
rand = "0.8"

# Utilities
bytes = "1.8"
futures = "0.3"
pin-project = "1.1"
thiserror = "2.0"
percent-encoding = "2.3"

# CLI
clap = { version = "4.5", features = ["derive"] }

# Macros
quote = "1.0"
syn = { version = "2.0", features = ["full"] }
proc-macro2 = "1.0"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true
